{
  "name": "work",
  "version": "1.0.0",
  "description": "This is a stand-alone reimplementation of one of the [Chrome fetch samples](https://github.com/GoogleChrome/samples/blob/gh-pages/fetch-api/fetch-reddit-demo.js). This is a work in progress whose intended use is a caching codelab.",
  "main": "gulpfile.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sw-precache": "^5.1.0"
  }
}
